- flash.each do |name, msg|
  %div{:class => "alert alert-#{name == :notice ? "success" : "error"}"}
    %button.close{:'data-dismiss' => "alert", :type => "button"} &#215;
    = content_tag :div, raw(msg), :id => "flash_#{name}" if msg.is_a?(String)